<script>
	import {capitalize} from '@svizzle/utils';

	import ExternalLink from '@svizzle/ui/src/ExternalLink.svelte';
	import IconDownload from '@svizzle/ui/src/icons/IconDownload.svelte';
	import Hamburger from 'app/components/Hamburger.svelte';

	import {version} from 'app/utils/version';

	const changelogUrl = 'https://github.com/nestauk/beis-indicators/blob/staging/CHANGELOG.md'; // FIXME

	export let segment;
	export let media;

	let open;

	const closeMenu = () => open = false;
</script>

<nav class:mobile={media.mobile}>
	{#if media.mobile}
		<Hamburger bind:open />
		<header>
			{segment ? capitalize(segment) : 'Home'}
		</header>
	{/if}
	{#if media.mobile && open || !media.mobile}
		<div class="backdrop" on:click={closeMenu}/>
		<menu>
			<div>
				<ul>
					<li class:selected='{segment === undefined}'>
						<a href='.'>Home</a>
					</li>
					<li class:selected='{segment === 'methodology'}'>
						<a href='methodology'>Methodology</a>
					</li>
					<li class:selected='{segment === 'guide'}'>
						<a href='guide'>Guide</a>
					</li>
					<li class:selected='{segment === 'indicators'}'>
						<a
							rel=prefetch
							href='indicators'
						>Indicators</a>
					</li>
				</ul>
			</div>
			<div>
				<ul>
					<li>
						<a href='/download'>
							<IconDownload
								size=20
								strokeWidth=1.5
							/> Download
						</a>
					</li>
					<li class:selected='{segment === 'feedback'}'>
						<a href='feedback'>Feedback</a>
					</li>
					<li>
						<ExternalLink href={changelogUrl} text={version}/>
					</li>
				</ul>
			</div>
		</menu>
	{/if}
</nav>

<style>
	nav, menu {
		width: 100%;
		height: 100%;

		display: flex;
		align-items: center;
		justify-content: space-between;
		z-index: 1000;
	}
	menu div, ul, li, a {
		height: 100%;
		font-family: 'Open Sans Regular', sans-serif;
	}

	ul {
		margin: 0;
		padding: 0;
		display: flex;
		align-items: center;
	}

	li {
		display: block;
		float: left;
		margin-right: 0.5rem;
		padding: 1em 0.5em;
	}

	li.selected {
		position: relative;
		display: inline-block;
		color: var(--color-link);
	}

	li.selected::after {
		position: absolute;
		content: '';
		width: calc(100% - 1em);
		height: 2px;
		background-color: var(--color-selected);
		display: block;
		bottom: -1px;
	}

	li .selected a {
		font-family: 'Open Sans Semibold', sans-serif;
	}

	a {
		text-decoration: none;
		display: block;
		user-select: none;
	}

	nav.mobile {
		justify-content: start;
	}
	.mobile header {
		margin: 0 auto;
	}
	.mobile .backdrop {
		position: fixed;
		top: 0;
		left: 0;
		width: 100vw;
		height: 100vh;
		background: rgba(128, 128, 128, 0.5);
		z-index: 10001;
	}
	.mobile menu {
		position: absolute;
		height: auto;
		display: block;
		background: white;
		border: 1px solid black;
		width: 84vw;
		left: 8vw;
		bottom: 8vw;
		z-index: 10002;
	}
	.mobile ul {
		flex-direction: column;
	}
	.mobile ul {
		flex-direction: column;
	}
</style>
