import {writable} from 'svelte/store';

export const colorCorrectionOptions = writable({
	active: true,
	invert: 0,
	colorModel: 'identity',
	'hue-rotate': 0,
	grayscale: 0,
	brightness: 100,
	contrast: 100
});

export const cvdFilter = writable('');
