import {writable} from 'svelte/store';

export const colorCorrectionOptions = writable({
	active: true,
	simulate: 'identity',
	invert: 0,
	colorModel: 'identity',
	'hue-rotate': 0,
	grayscale: 0,
	brightness: 100,
	contrast: 100
});

export const colorCorrection = writable('');

export const cvdSimulation = writable('');
