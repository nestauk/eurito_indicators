import {writable} from "svelte/store";

export const fontFamily = writable();

export const fontScaling = writable(1.0);

export const fontVariationSettings = writable();

export const letterSpacing = writable();

export const wordSpacing = writable();

export const lineHeight = writable();
