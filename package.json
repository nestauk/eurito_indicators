{
  "description": "EURITO indicators",
  "license": "MIT",
  "name": "eurito_indicators",
  "scripts": {
    "deploy": "cd ui && npm install && npm run makedata && npm run export"
  },
  "version": "0.0.1"
}
